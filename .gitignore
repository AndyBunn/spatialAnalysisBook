# RStudio
.Rproj.user/
.Rhistory
.RData
.Ruserdata

# Quarto internals
/.quarto/
**/*.quarto_ipynb

# knitr cache
_cache/
*.utf8.md
*.knit.md

# intermediate files
*.log
*.aux
*.toc
*.out

# misc
todo.txt
Steps2Publication.md
.DS_Store
data.zip
.positai
rScripts/buildMeuseCovariates.R
data/ESA_WorldCover_meuse.tif

# docs/ is built and deployed by GitHub Actions (.github/workflows/publish.yml),
# so it no longer needs to be committed.
docs/

# _freeze is kept so CI can reuse cached outputs and skip re-running unchanged chapters